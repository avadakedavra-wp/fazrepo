{
  "name": "cli",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "clean": "cargo clean",
    "lint": "cargo clippy",
    "install": "cargo install --path ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
} 